# 2026-09-18 SO101 SmolVLA 微调

> 更新：10,000步已于2026-09-18 19:08:56（北京时间）完成，最终eval_loss=0.0915。以下保留启动时记录；最新完整交接见 `../docs/交接文档.md`，完成证据见 `remote-records/logs/train10k.summary.log`。

状态：200步测速、离线验证、检查点保存均通过；2026-09-18 17:57（北京时间）已从第200步恢复，后台继续训练到总计10,000步。尚未完成正式训练。

测速实测：200步训练88秒，含最后验证和保存约90秒；稳定更新用时约0.428秒/步（含0.30秒主动让出时间，不含数据加载），单步读数据约0.004秒。PyTorch峰值分配2.06GiB，nvidia-smi本任务约2.6GiB。最后20步平均训练loss=0.335，32个验证样本eval_loss=0.1412；不是实机成功率。

预计正式剩余训练约75–90分钟，取决于共享GPU负载和检查点保存。脚本已验证能重新加载200步模型及优化器状态并继续更新。后台启动PID=2063360（shell父进程，非GPU显示的宿主机PID）。

## 数据
- 仅使用9月18日的14、15、16、17、18、20、22、23、24、25，共67段、35,651帧。
- 9月16日数据因相机安装位置和焦距不同，本轮排除。
- 20个视频均完成逐帧解码，帧数与数据表一致；状态与动作无NaN/Inf。
- 67段结束画面可见黄方块在网状容器内；只检查终点不代表逐段完整动作已人工审核。
- 合并副本：ChomCUI/yellow_cube_0918_v1；原始数据不改写。
- 语言标注：Pick up the yellow cube and place it into the mesh container.
- 相机映射：images.3（外部）→images.camera1；images.1（腕部）→images.camera2。
- 官方代码按episode划分56段训练、11段验证：验证集是24最后1段和25全部10段，并非随机按帧拆分。

## 环境与资源限制
- ssh myserver，A100 80GB；启动前已有约19GB显存占用。
- 独立目录：/vepfs-mlp2/mlp-public/huyaoqing/so101_vla_20260918
- Python 3.12.13、PyTorch 2.7.1+cu118、torchvision 0.22.1、LeRobot本地0.6.0源码快照。
- 依赖一致性检查和CUDA矩阵乘法测试通过。
- batch=4，2个数据worker，CPU affinity限制4个逻辑核，线程数2，nice=19。
- PyTorch allocator上限约16GB；GPU全局空闲显存低于12GiB时仅退出本任务。
- 每次训练更新后休息0.30秒；这些措施减少竞争，但共享GPU不能保证其他作业完全不受影响。
- 不停止其他作业、不修改全局GPU设置、不修改他人环境。

## 执行计划与日志
- 200步验证：outputs/smoke200，logs/smoke.log。
- 测速通过后，从200步checkpoint恢复并继续到总计10,000步：outputs/train10k，logs/train10k.log。
- 每1,000步保存和离线验证（最多128验证样本）；实际正式训练已启动，batch保持4。
- 200步测速采用框架自动缩放的短周期学习率；恢复后调度器按10,000步重新构建，不应将这次短测速视作完整学习率方案的质量对照。
- 不自动进行实机运动；先核对部署预处理、动作归一化与相机映射，并修复采集时出现的通信超时问题。
- 数据来源映射：远端data/episode_provenance.json。
